[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# No functions section - use default location
# Functions will be auto-detected in the 'functions' directory

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
